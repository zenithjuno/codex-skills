<!-- grill-to-build:thai-docx-skill:start -->
## Active staged build: thai-docx-skill

- Control file: `docs/plans/active/thai-docx-skill/BUILD-CONTROL-thai-docx-skill.md`
- Begin build work by reading that control file (validate, then read the current stage only).
- Read only the current plan stage and its named contract sections in `BLUEPRINT-thai-docx-skill.md`.
- At stage close and on every approved CHG, reconcile the registered current-truth surfaces: state what became true AND what stopped being true.
- Never bulk-read `docs/plans/active/thai-docx-skill/history/` or discover controls by glob.
- Edit only paths declared by the current stage; classify extra paths against the Project Map first.
- thai-math-docx MATH modules are protected: no math module may load on the thai-docx general path.
- Run the stage's declared focused + regression checks before its PASS GATE.
- Checkpoint only managed paths after the stage passes; commit only on explicit user ask.
<!-- grill-to-build:thai-docx-skill:end -->
