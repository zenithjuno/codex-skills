interface:
  display_name: "soffice Runtime Fix"
  short_description: "Diagnose and repair bundled LibreOffice rendering"
  default_prompt: "Use $soffice-runtime-fix to identify the failing DOCX render stage, repair only the active bundled soffice path when warranted, and verify Thai and equation rendering."
policy:
  allow_implicit_invocation: true
