# Build Changelog — Intro ยูเนียนของเซต

<!-- ===== ZONE A: STATE — overwrite in place, keep this short & constant-size ===== -->
## STATE — where we are now (updated: 2026-07-16)
- Current stage: Stage 11 — Union full master handout v4.1 (complete; awaiting Word review)
- Done: stages 0–4 ✓ | PNG goldens ✓ | SVG source assets ✓ | component DOCX ✓ | full master DOCX ✓ | diagram-assets index ✓
- Open / next: teacher opens the full Union master in Microsoft Word; Intersection needs its missing stages 0–2 designed before it can have an equivalent master.
- Last change: PRG-023 — combined Union handout made current

## OPEN CHANGES
- (none)

## LOG  (append-only, newest at bottom)

### [2026-07-16] CHG-007 · Union v4.1 Euler preservation · APPROVED
- Plan for v4 said: apply the current visual grammar to refreshed Euler diagrams as well as Venn diagrams.
- Found during teacher review: the v4 preview changed the spatial composition of E2–E4 more than the visual grammar required, even though the set relations remained mathematically correct.
- Decision: preserve the accepted v3 Euler geometry and relative placement exactly; only repair labels that lie inside their own set. Keep the v4 circular Venn diagrams and all current PNG-golden-first QA. Approved by: ครูผู้สอน, 2026-07-16.

### [2026-07-15] CHG-001 · approved content refinement · affects Material Design §Block 1 and §Block 2
- Plan said: ตัวอย่างเซตว่างใช้ `∅` ในตัวอย่างครูเดิม และมีข้อเสนอใช้ `{ }` ในแบบฝึก.
- Decision: ใช้ `{ }` ทั้งตัวอย่างครูและแบบฝึกในช่วง intro; เก็บ `∅` ไว้สำหรับบทที่อธิบายว่าเป็นสัญลักษณ์แทนเซตว่างภายหลัง.
- Reason: ให้ผู้เรียนเข้าใจจากรูปปีกกาว่าเป็นเซตที่ไม่มีสมาชิก ก่อนท่องสัญลักษณ์.
- Approved by: ครูผู้สอน, 2026-07-15.

### [2026-07-15] PRG-001 · Stages 1–2 — สร้างและตรวจ DOCX · PASS
- Built: `บทนำ_ยูเนียนของเซต_ตัวอย่างและแบบฝึกหัด.docx` มีหน้าสอนพร้อมตัวอย่างครู 6 ข้อ และหน้าฝึกหาผลยูเนียน 10 ข้อแบบเว้นคำตอบ.
- Verified: `fix-thai-font` repair + check ผ่าน; font-default และ insertion-safety audits ผ่าน; OMML audit พบสมการแก้ไขได้ 24 ชุด (รวมเศษส่วน 2 ชุด), ไม่มีภาพสมการ.
- Verified content: มี page break 1 จุดสำหรับแยกหน้าสอน/หน้าฝึก และไม่พบสัญลักษณ์ `∅` ใน `word/document.xml`; กรณีเซตว่างจึงใช้ `{ }` ตามที่อนุมัติ.
- You saw: ไฟล์พร้อมเปิดใน Microsoft Word เพื่อตรวจ visual truth; การตรวจอัตโนมัติไม่ได้อ้างผลการจัดบรรทัดจาก LibreOffice.

### [2026-07-15] CHG-002 · approved layout refinement · affects Material Design §Recommended page architecture
- Plan said: Block 2 เป็นหน้าฝึกหาผลลัพธ์ของตนเองโดยยังไม่กำหนดคอลัมน์.
- Decision: จัด Block 2 เป็นตาราง fixed `5 × 2` แบบไม่มีเส้นขอบ เรียงข้อ 1–10 จากซ้ายไปขวาแล้วลงแถว.
- Reason: โจทย์สั้น มีคำตอบหนึ่งบรรทัด และครูยืนยันว่า 2 คอลัมน์เหมาะกว่า.
- Approved by: ครูผู้สอน, 2026-07-15.

### [2026-07-15] PRG-002 · Stage 2 — Block 2 แบบ 2 คอลัมน์ · PASS
- Built: หน้าฝึกเป็นตาราง fixed `5 × 2` ไม่มีเส้นขอบ; มี 5 แถว 2 คอลัมน์ รวม 10 ช่อง และเรียงข้อแบบ row-major.
- Verified: font normalization/check, font-default, insertion-safety และ OMML audits ผ่านซ้ำ; สมการแก้ไขได้ 24 ชุด และไม่มีภาพสมการ.
- Verified structure: XML ยืนยัน `practice_rows=5`, `practice_grid_columns=2`, `practice_cells=10`, `nil_borders=6`, page break 1 จุด และไม่พบ `∅`.
- You saw: DOCX ฉบับเดียวกันถูกสร้างทับเป็น layout 2 คอลัมน์; Microsoft Word remains the visual truth for spacing/wrapping.

### [2026-07-15] PRG-003 · Stage 3 — PNG preview ของ Block 3A/3B · PASS
- Built: PNG review-only ความละเอียด 600 dpi จำนวน 7 ภาพ พร้อม contact sheets สำหรับ Block 3A (เวนน์ 4 ภาพ) และ Block 3B (ออยเลอร์ 3 ภาพ) ใน `set-union-block3-preview-v1/`.
- Verified semantics: E1 มี `A` แยกจาก `B` และ `C` ทับ `B`; E2 มี `B ⊂ A` และ `C` อยู่นอก `A`; E3 มี `A ⊂ B` และ `C` ทับ `B` โดยไม่ทับ `A`.
- Visual check: ตรวจ contact sheets และ V2 รายภาพแล้ว; แก้ caption ให้ใช้ glyph `∪` ถูกต้องและย้ายป้าย `C` ไม่ให้ชนขอบวง.
- Gate: PNG เหล่านี้เป็น preview เพื่อการตัดสินใจเรื่องลำดับ/ภาพเท่านั้น ยังไม่ได้สร้าง SVG หรือแทรก DOCX visual block.

### [2026-07-15] PRG-004 · Stage 3 — Fine-tune วง C ใน preview ออยเลอร์ · PASS
- Found during visual review: วง `C` ของ E1 และ E3 ล้ำกรอบ `U` เพราะตั้งค่าให้ทับวง `B` กว้างเกินไป.
- Fixed: ลดรัศมีและเลื่อนวง `C` เข้ากรอบ โดยยังรักษา E1 `B ∩ C ≠ ∅`, E2 `C ∩ A = ∅`, และ E3 `B ∩ C ≠ ∅` แต่ `A ∩ C = ∅`.
- Verified: re-render ที่ 600 dpi และตรวจ Block 3B preview; ไม่มีวงล้ำกรอบ `U`.

### [2026-07-15] PRG-005 · Stage 3 — Balance โครง E2/E3 · PASS
- Found during teacher review: `C` ใน E2/E3 ดูเป็นวงที่วางเพิ่มภายหลัง เพราะแขวนอยู่ริมขวาและน้ำหนักภาพไม่สมดุล.
- Fixed: E2 จัด `C` เป็นกลุ่มทางซ้ายและ `A ⊃ B` ทางขวา; E3 ให้ `C` คาบด้านซ้ายของ `B` โดยไม่แตะ `A`. ปรับป้าย `A`,`B` ในวงซ้อนให้ไม่ชนเส้นขอบ.
- Verified: re-render ที่ 600 dpi; ความสัมพันธ์เชิงเซตเดิมและเงื่อนไข geometry ทุกข้อผ่าน.

### [2026-07-15] PRG-006 · Stage 3 — Lock Block 3 preview · PASS
- Decision: ครูรับ composition และลำดับภาพ Block 3A/3B หลัง fine-tune geometry ของวง `C`.
- Locked content: Venn 4 ข้อตาม preview และ Euler E1–E3 ที่มี `C` เป็น distractor/overlap อย่างมีความหมาย.
- Next: ออกแบบ Block 4 ให้ใช้หลักการเดียวกันแต่ไม่ซ้ำ expression หรือคู่ containment ของ Block 3.

### [2026-07-15] PRG-007 · Stage 4 — PNG เฉลย Block 4 · PASS
- Built: PNG เฉลย 600 dpi จำนวน 7 ภาพ พร้อม contact sheets Block 4A (เวนน์ D1–D4) และ Block 4B (ออยเลอร์ D5–D7).
- Verified semantics: D5 มี `C` ทับ `A` แต่ไม่ทับ `B`; D6 มี `A ⊂ C` และ `B` แยกจาก `C`; D7 มี `B ⊂ C` และ `A` ทับ `C` โดยไม่ทับ `B`.
- Visual check: ตรวจ contact sheets แล้ว; D6/D7 ใช้ composition สมดุลแบบ C เป็นวงนอก ต่างจาก containment ใน Block 3.
- Gate: ภาพมีสีเทาและ caption คำตอบเพื่อ review เท่านั้น; template นักเรียนจะลบ fill และ caption เฉลยเมื่อครูรับ flow.

### [2026-07-15] PRG-008 · Stage 4 — Revise D5–D7 to prevent false pattern-matching · PASS
- Found during teacher review: การสลับเพียงชื่อเซตใน D5–D7 อาจทำให้เด็กสับสนเพราะเปรียบกับภาพเดิม ไม่ได้วัดความเข้าใจยูเนียน.
- Fixed: D5 ใช้ geometry `A–B–C` แบบ E1 แต่ถาม `A ∪ C`; D6 ย้าย `A ⊂ C` ไปซ้ายและ `B` ไปขวา; D7 ใช้ `A` ทับ `B` แล้วถาม `A ∪ B` โดยมี `C` แยกออกไป.
- Verified: re-render 600 dpi และ semantic geometry assertions ผ่านสำหรับความสัมพันธ์ใหม่ทั้งสามข้อ.

### [2026-07-15] PRG-009 · Stage 4 — D7: containment เป็น context ของ region · PASS
- Decision: D7 ใช้ `B ⊂ C` พร้อม `A ∩ B ≠ ∅` และ `A ∩ C ≠ ∅`, แต่ยังถาม `A ∪ B`.
- Built: ขยายวง `B` ไปทับ `A` โดยยังอยู่ภายในวง `C`; เฉลยระบาย A/B เท่านั้น จึงเหลือส่วน C-only เป็นสีขาว.
- Verified: geometry assertion ยืนยัน `B ⊂ C`, `A ∩ B ≠ ∅`, และ `A ∩ C ≠ ∅`; re-render PNG 600 dpi ผ่าน.

### [2026-07-15] PRG-010 · Stage 4 — Lock Block 4 answer flow · PASS
- Decision: ครูอนุมัติ Block 4 หลังปรับ D5–D7 โดยใช้ geometry เปลี่ยนจริง ไม่สลับชื่อเซตหลอก.
- Next: สร้าง V1 SVG-text spike แบบ compact ก่อนผลิต visual block ทั้งชุด.

### [2026-07-15] PRG-011 · Stage 5 — V1 compact SVG-text spike · PASS (structural; awaiting Word visual gate)
- Built: `SPIKE_V1_ยูเนียน_SVGtext12pt_compact80pct.docx` ฝัง SVG เพียงชิ้นเดียวที่ `3.60 × 2.16 in` (80% ของ baseline `4.50 × 2.70 in`); labels `A,B,U` เป็น SVG text `12 pt` และไม่มีพื้นหลังขาว.
- Golden reference: PNG 600 dpi จาก geometry เดียวกันแยกเก็บไว้เพื่อเทียบหลัง Word Convert to Shape.
- Verified: font normalizer/check, font-default และ insertion-safety audits ผ่าน; package audit ยืนยันมี `image1.svg` เพียงชิ้น ไม่มี PNG/raster media; SVG audit ยืนยัน fill/outline 2 คู่ geometry ตรงกัน และ labels เป็น direct SVG text.
- Expected exception: OMML audit รายงาน drawing image 1 ชิ้นและ fail ตาม policy เพราะ SVG diagram ถูกนับเป็น image; ตรวจ package แล้วว่าเป็น SVG แก้ไขได้ ไม่ใช่ภาพสมการ raster และ spike ไม่มีสมการในเนื้อหา DOCX.
- Gate: ต้องให้ครูตรวจขนาดใน Microsoft Word และทดสอบ Convert to Shape/Object ก่อนขยายไปทั้งชุด.

### [2026-07-15] CHG-003 · approved test refinement · affects Stage 5 compact SVG size
- Prior spike: `3.60 × 2.16 in` (80% ของ compact baseline เดิม).
- Decision: ลด geometry ลงอีก 80% เป็น `2.88 × 1.728 in` เพื่อทดสอบขนาดที่ครูคาดว่าจะพอดีกับหน้าจริง; คง SVG label ที่ `12 pt`.
- Approved by: ครูผู้สอน, 2026-07-15.

### [2026-07-15] PRG-012 · Stage 5 — V1 compact64 SVG-text spike · PASS (Word gate passed)
- Built: `SPIKE_V1_ยูเนียน_SVGtext12pt_compact64pct.docx` ที่ geometry `2.88 × 1.728 in`; label SVG ตรง `12 pt`, canvas โปร่งใส และ `U` อยู่ใกล้กลุ่มวง.
- Gate: ครูเปิดใน Microsoft Word และยืนยันว่าขนาดนี้พอดีกับ 2 คอลัมน์หลัง Convert to Shape/Object.
- Decision carried forward: ใช้ profile นี้เป็นขนาดตั้งต้นมาตรฐานของโจทย์ภาพ operation ของเซตในตาราง 2 คอลัมน์.

### [2026-07-15] CHG-004 · approved production-format refinement · affects visual-question asset format
- Prior approach: compact64 ได้รับการยืนยันเฉพาะ V1 เดี่ยว.
- Decision: ยกระดับ `2.88 × 1.728 in` + direct SVG label `12 pt` เป็นมาตรฐานสำหรับโจทย์ภาพ operation ของเซตแบบ 2 คอลัมน์ และทำ spike ที่ใส่หลายภาพ inline ในตารางจริง.
- Reason: ครูยืนยันว่าขนาดนี้พอดีกับ layout สองคอลัมน์บนเครื่อง Word จริง.
- Approved by: ครูผู้สอน, 2026-07-15.

### [2026-07-15] PRG-013 · Stage 6 — Block 3A V1–V4 SVG inline table spike · PASS (structural; awaiting Word visual gate)
- Built: `set-union-block3a-two-column-svg-spike/SPIKE_Block3A_V1-V4_SVGtext12pt_2column.docx` มี V1–V4 เฉลยแรเงาในตาราง fixed 2 × 2 ไม่มีเส้นขอบ; รูปทุกชิ้น inline ที่ `2.88 × 1.728 in` ตามมาตรฐานใหม่.
- Verified: package มี SVG 4 ชิ้นและไม่มี PNG/raster media; SVG ทุกชิ้นใช้ lower opaque fill `#D9D9D9`, upper matched outlines, direct `Cambria Math` text `12 pt`, ไม่มีพื้นหลังขาว/opacity/mask/filter/image. XML ยืนยัน inline extent ทั้ง 4 ชิ้นเป็น `2633472 × 1580083` EMU และตารางมี 2 แถว 4 cells.
- Verified typography: `fix-thai-font` repair + check, font-default audit และ insertion-safety audit ผ่าน.
- Expected exception: OMML audit รายงาน `oMath=0` และ drawing images 4 ชิ้น เพราะเอกสารนี้เป็น format spike ของ SVG diagram ไม่ใช่โจทย์สมการ; package audit ยืนยันว่า image ทั้งหมดเป็น SVG editable source ไม่ใช่ภาพสมการ raster.
- Gate: ครูเปิดใน Microsoft Word เพื่อตรวจ spacing/size และ Convert to Shape/Object ก่อนใช้ format นี้ผลิต Block 3/4 ฉบับนักเรียน.

### [2026-07-15] CHG-005 · approved response-line typography · affects Block 3/4 visual-question layout
- Prior assumption: จุดไข่ปลาอาจใช้ Latin/default run เหมือนข้อความ administrative ทั่วไป.
- Decision: จุดไข่ปลาใต้รูปทุกข้อใช้ literal `.` ใน run แบบ all-slot `TH Sarabun New` 16 pt.
- Reason: ครูต้องการให้เส้นตอบกลมกลืนกับงานภาษาไทย ไม่ใช่ Cambria 12 pt.
- Approved by: ครูผู้สอน, 2026-07-15.

### [2026-07-15] PRG-014 · Stage 7 — ใบงาน Block 3/4 แบบ 2 คอลัมน์ · PASS (structural; awaiting final Word review)
- Built: `set-union-blocks34-two-column/แบบฝึก_แผนภาพยูเนียน_Block3-4_2คอลัมน์_SVG.docx`; Block 3 มีภาพแรเงา V1–V4/E1–E3 ให้เขียนยูเนียน และ Block 4 มี D1–D7 ไม่แรเงาให้ระบายตามนิพจน์. ทั้งสอง block อยู่ในตาราง fixed 4 × 2 ไม่มีเส้นขอบ คนละหน้า.
- Verified: รูปทั้งหมด 14 ชิ้นเป็น transparent SVG `2.88 × 1.728 in` ที่มี direct `Cambria Math` 12 pt labels; package ไม่มี PNG/raster media. Block 3 ใช้ opaque lower fills + matched upper outlines; Block 4 ไม่มี fill เพื่อเป็น template นักเรียน.
- Verified typography: `fix-thai-font` repair + check, font-default, insertion-safety ผ่าน. ตรวจ XML เพิ่มเฉพาะจุดตอบ: มี dot run 14 ชุด และทุกชุดเป็น `TH Sarabun New` ทุก font slot, `16 pt` (`w:sz=32`, `w:szCs=32`) พร้อม complex-script toggle.
- Expected exception: OMML audit พบ OMML 7 ชุดสำหรับนิพจน์ Block 4 และพบ drawing images 14 ชิ้น; audit จึงเตือนตาม policy แต่ package audit ยืนยันว่าทั้ง 14 ชิ้นเป็น SVG editable source ไม่ใช่ภาพสมการ raster.
- Gate: Microsoft Word ของครูเป็น visual truth สำหรับ spacing จริง; ไฟล์พร้อมให้ครูปรับต่อราว 5–10% ตามที่อนุมัติ.

### [2026-07-15] CHG-006 · approved content refinement · affects Block 3/4 visual exercises
- Prior worksheet: มี 7 ข้อต่อ block; Block 3 E3 แรเงา `B` เพื่อตอบ containment-result แบบเดียวกับ E2.
- Decision: เปลี่ยน Block 3 ข้อ 7 (E3) เป็นแรเงา `A ∪ C`; เพิ่มข้อ 8 ทั้งสอง block ด้วย Euler containment สามชั้น `A ⊂ B ⊂ C`. Block 3 แรเงา `B` เพื่อให้อ่าน `A ∪ B`; Block 4 ให้ระบาย `B ∪ C` จึงได้วง `C`.
- Reason: ตัด containment-result ซ้ำใน Block 3 และเติม capstone ใหม่ที่เป็นลูกโซ่ containment; การเลือก `B ∪ C` ทำให้เห็นบันได `A ∪ B → B` แล้ว `B ∪ C → C`.
- Approved by: ครูผู้สอน, 2026-07-15.

### [2026-07-15] PRG-015 · Stage 7 — B3-8/B4-8 containment answer spike · PASS (structural; awaiting Word review)
- Built: `set-union-b38-b48-answer-spike/SPIKE_Block3-8_Block4-8_เฉลย_SVG_2คอลัมน์.docx` เป็นตาราง 1 × 2 ที่แสดงเฉลยภาพคู่กัน. ทั้งสองช่องใช้ Euler geometry เดียว `A ⊂ B ⊂ C`; B3-8 แรเงา `B` พร้อม `A ∪ B = B`, B4-8 แรเงา `C` พร้อม `B ∪ C = C`.
- Verified: PNG golden 600 dpi ถูกสร้างก่อน SVG ทั้งสอง; DOCX package มี transparent SVG 2 ชิ้นเท่านั้น ไม่มี PNG/raster media. SVG ใช้ fill layer เฉพาะวงคำตอบ (`B` หรือ `C`), outlines ของ `A/B/C` ที่ geometry เดียวกัน, และ direct `Cambria Math` labels `12 pt`.
- Verified typography: `fix-thai-font` repair + check, font-default และ insertion-safety audits ผ่าน. OMML audit พบสมการ editable 3 ชุดและ drawing SVG 2 ชิ้น; warning image เป็น expected exception และ package audit ยืนยันว่าไม่ใช่ raster equation.
- Gate: ครูดู geometry/ลำดับของข้อ 8 พร้อมเฉลยก่อน integrate เข้าใบงาน Block 3/4 v2.

### [2026-07-15] PRG-016 · Stage 7 — integrate Block 3/4 v2 · PASS (structural; awaiting final Word review)
- Built: `set-union-blocks34-two-column-v2/แบบฝึก_แผนภาพยูเนียน_Block3-4_2คอลัมน์_v2_SVG.docx`; เก็บ v1 ไว้ไม่แตะต้อง. ทั้ง Block 3 และ Block 4 เป็นตาราง fixed 4 × 2 เต็มแถว รวม 8 ข้อต่อ block.
- Applied approved content: E3 เปลี่ยนเป็นแรเงา `A ∪ C`; E4 ใช้ `A ⊂ B ⊂ C` และแรเงา `B` สำหรับการอ่าน `A ∪ B`; D8 ใช้ containment เดียวกันเป็น template เปล่า พร้อมนิพจน์ editable `B ∪ C`.
- Verified: package มี SVG 16 ชิ้น ไม่มี PNG/raster media; inline extent ทั้ง 16 ชิ้นเป็น `2.88 × 1.728 in`; SVG labels direct `Cambria Math` 12 pt และ canvas transparent. ตรวจ selection เพิ่ม: E3 fill คือ `A/C`, E4 fill คือ `B`, D8 ไม่มี fill.
- Verified typography: `fix-thai-font` repair + check, font-default และ insertion-safety ผ่าน. dot lines 16 ชุดผ่าน XML audit ว่า `TH Sarabun New` ทุก font slot, 16 pt. OMML audit พบ expression editable 8 ชุด และ SVG drawings 16 ชิ้นเป็น expected image warning; package audit ยืนยันไม่มีภาพสมการ raster.
- Gate: Microsoft Word ของครูเป็น visual truth สำหรับ spacing/placement; v2 พร้อมให้เปิดตรวจหรือจัดต่อ.

### [2026-07-15] PRG-017 · Stage 7 — containment-scale repair in v3 · PASS (structural; awaiting Word review)
- Found during teacher Word review: E4/D8 ใน v2 มีวงสามชั้นหดและกองที่มุมซ้ายบน ขณะที่กรอบ `U` มีขนาดปกติ; label 12 pt จึงชนวง.
- Root cause: wrapper v2 ส่ง compact coordinates เข้า shared renderer ที่คูณ scale `0.44` อยู่แล้ว จึง scale วงและตำแหน่งซ้ำเป็น `0.44 × 0.44`; ตัวอย่าง `C` จาก `(105, 60, 76, 39)` กลายเป็น `(46.2, 26.4, 33.44, 17.16)` ใน SVG.
- Fixed: สร้าง v3 แยกจาก v2 และแปลง desired compact chain coordinates กลับเป็น source-grid coordinates ก่อนให้ renderer scale เพียงครั้งเดียว. ปรับวง `C` ให้อยู่ในกรอบหลักอย่างปลอดภัย พร้อมช่องว่างสำหรับ labels 12 pt.
- Verified: golden PNG E4 แสดง containment `A ⊂ B ⊂ C` เต็มกรอบ; SVG/package audit ยืนยัน 16 SVG ไม่มี PNG/raster, direct labels 12 pt, dot lines Thai 16 pt; font/default/insertion audits ผ่าน. OMML audit มี 8 expression editable และ drawing SVG 16 ชิ้นเป็น expected image warning.
- Gate: ครูตรวจ v3 ใน Microsoft Word ก่อนใช้งานต่อ; v1/v2 คงไว้เป็นหลักฐานและไม่ได้ถูกเขียนทับ.

### [2026-07-15] PRG-012 · Stage 5 — V1 compact64 SVG-text spike · PASS (structural; awaiting Word visual gate)
- Built: separate compact64 variant at `2.88 × 1.728 in`, with geometry reduced to 80% of compact80 but label text retained at direct `12 pt`.
- Verified: font normalization/check, font-default and insertion-safety audits passed; SVG/package audit confirms exactly one SVG media item, no PNG/raster media, transparent canvas, two opaque fills + matching two outlines, and direct editable labels.
- Golden reference: PNG rendered at 600 dpi from the exact scaled geometry.
- Gate: teacher checks real Word placement and post-conversion object size; compact80 variant remains preserved and untouched.

### [2026-07-16] PRG-018 · Stage 8 — Union Block ดูภาพ v4 PNG golden preview · COMPLETE / AWAITING REVIEW
- Built: `set-union-block3-answer-preview-v4/` with eight 600 dpi individual PNG goldens and `เฉลย_Block3_ยูเนียน_8ข้อ_600dpi.png` as the review sheet; v1–v3 source and output folders remain unchanged.
- Applied the current visual standard without changing the approved teaching order: V1–V4 are circular Venn templates; E1–E4 are elliptical Euler relationships; every set label anchor is outside the boundary it names; `U` remains at the universe-frame corner.
- Restored E3 to its approved relationship: `A ⊂ B`, while `C` intersects `B` but not `A`; the answer fill remains `A ∪ C`, so `B` is a meaningful unselected distractor.
- Automated checks passed: all label-exterior anchors; Venn/Euler shape-type split; E1 disjoint selected sets with `C` intersecting `B`; E2 containment; E3 containment/overlap/disjointness; E4 chain `A ⊂ B ⊂ C`; every PNG is 600 dpi from the exact geometry.
- Pending teacher gate: inspect the answer sheet’s visual flow before main SVG/text assets are produced.

### [2026-07-16] PRG-019 · Stage 8 — Union Block ดูภาพ v4.1 PNG golden preview · COMPLETE / AWAITING REVIEW
- Built: `set-union-block3-answer-preview-v4.1/` with a new eight-item 600 dpi review sheet. The v4 directory remains preserved as the rejected composition variant.
- Restored the accepted v3 Euler geometry: E1 remains A-left / B-middle / C-right; E2 remains C-left with `B ⊂ A` on the right; E3 remains C-left overlapping outer B but not inner A; E4 retains the wide nested containment chain.
- Applied only the approved corrections: Venn V1–V4 remain circular, and the former inside-set labels for E2/B, E3/A, and E4/A now lie outside their own boundary.
- Automated relationship/label checks and 600 dpi output checks passed. Pending teacher gate: review this PNG sheet before matching SVG production.

### [2026-07-16] PRG-020 · Stage 9 — Union Block วาดภาพ v4.1 PNG golden preview · COMPLETE / AWAITING REVIEW
- Built: `set-union-block4-answer-preview-v4.1/` with eight individual 600 dpi answer goldens and `เฉลย_Block4_ยูเนียน_8ข้อ_600dpi.png` for review.
- Venn D1–D4 use the v4 circular templates. Euler D5–D8 preserve their accepted layout: D5 has A separate and C overlapping unused B; D6 has `A ⊂ C` left of a separate B; D7 has `B ⊂ C` with A overlapping B/C; D8 is the chain `A ⊂ B ⊂ C`.
- The answer fills show the requested union for teacher flow review only; the eventual student SVGs will be unshaded templates. Labels that had been inside their own set were moved outside without altering the ellipse geometry. D7/B received a final spacing adjustment to avoid the C outline.
- Automated checks passed: Venn/Euler shape split, exterior labels, D5 disjoint selected sets with unused overlap, D6/D7 containment, D7 A/B overlap, D8 triple containment, and 600 dpi output dimensions. Pending teacher gate: review before matching SVG/template production.

### [2026-07-16] PRG-021 · Stage 10 — Union v4.1 two-column worksheet · COMPLETE
- Built: `diagram-assets/union/current/worksheet-v4.1/แบบฝึก_แผนภาพยูเนียน_Block3-4_2คอลัมน์_v4.1_SVG.docx`.
- Page 1 is Block ดูภาพ with eight approved shaded diagrams; page 2 is Block วาดภาพ with eight unshaded SVG templates and editable OMML prompts. Both pages use fixed 4 × 2 borderless tables and Thai-styled dotted response rows.
- Every diagram was rendered first as a 600 dpi PNG golden, then emitted as a matching transparent compact SVG (`207.36 × 124.416 pt`, direct `Cambria Math` 12 pt labels). The DOCX embeds 16 SVG media entries and zero PNG media entries.
- Checks passed: set-relation and label-exterior geometry; SVG no-raster/no-opacity/no-mask/no-clip/no-filter audit; package SVG count; 8 native OMML objects; Thai-font normalizer/check; docDefaults/Normal audit; Thai insertion-safety audit.
- Known generic-audit exception: the OMML audit's image warning is expected because it does not distinguish intentional SVG diagrams from equation images. Package inspection confirms the 16 image entries are all SVG, while all eight equations are native OMML.

### [2026-07-16] PRG-022 · Stage 10 — diagram-assets current/legacy orientation · COMPLETE
- Created `diagram-assets/START-HERE.md` as the single entry point for the set-operation diagram zone. It links the current Union worksheet, its two teacher-answer preview sheets, and the current Intersection worksheet.
- Moved only the two current Union v4.1 preview directories under `diagram-assets/union/current/answer-previews/`; their builders now reproduce them there. Historical asset folders were deliberately left in their original locations so the earlier build-log paths remain valid.
- The index explicitly marks `current` assets versus legacy v1–v3 folders and rejected/exploration variants. Future diagram builds should place the teacher-facing current outputs under `diagram-assets/<operation>/current/` and add a link to this index.

### [2026-07-16] PRG-023 · Stage 11 — Union full master handout v4.1 · COMPLETE
- Built: `diagram-assets/union/current/full-handout-v4.1/ใบงาน_ยูเนียนของเซต_ครบหัวข้อ_v4.1.docx`.
- The master establishes the complete learning flow in one student-facing file: operation bridge and definition → six worked roster examples → ten roster-method exercises → eight shaded read-the-diagram questions → eight unshaded shade-the-diagram questions.
- The roster section preserves the approved content while adopting the now-confirmed Thai-styled dotted response-line rule. The visual sections embed the same 16 SVG sources as the diagram-only component.
- Checks passed: 32 native OMML objects including two editable fractions; 16 SVG media entries and zero PNG media entries; Thai-font normalizer/check; font-default and insertion-safety audits. The generic OMML image warning remains an expected SVG-diagram exception; package inspection confirmed no equation rasterization.
- Updated `diagram-assets/START-HERE.md`: the full master is now the first/current Union file; the diagram-only DOCX is retained as a reusable component. Added `TEMPLATE-set-operation-full-handout.md` to distinguish the complete 0–4 learning flow from the pre-existing visual-only template.
