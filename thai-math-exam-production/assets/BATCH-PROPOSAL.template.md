# Batch <NN> Proposal — <ชื่อข้อสอบ>

`Status:` `drafting | proposed | approved`
`Items:` `Q0x, Q0y, ...`  (เติม variant id หลังอนุมัติ เช่น `Q01A approved`)
`Workload:` `Easy 1 + Easy 1 + Medium 2 = 4`

> `Workload` = ภาระ **ร่างและตรวจ** ของครูใน batch นี้ (Easy 1 · Medium 2 · Hard 3) — ไม่ใช่คะแนนสอบ.
> เป้าหมาย **3–4 หน่วยต่อ batch**. ถ้าเกินหรือต่ำกว่า เขียนเหตุผล override ไว้ในบรรทัดนี้.
> (บังคับที่เทมเพลต + teacher review ไม่ใช่ validator — batch ไม่อยู่ใน JSON.)

---

## <ITEM_ID> — <easy|medium|hard> — <STATUS>

**เป้าหมาย:** ข้อนี้วัดอะไร และอยู่ตรงไหนของ blueprint.

<!-- parallel:start -->
**Item anchor:** `<ข้ออ้างอิงในชุดต้นฉบับ>` — บทบาทเดียวกัน.
**คงไว้ (preserve):** ทักษะหลัก · จำนวน/ชนิดการตัดสินใจ · misconception ที่วินิจฉัย · ภาระคำนวณมือ.
**เปลี่ยน (transform):** ตัวเลข/สัมประสิทธิ์/ราก · บริบท/ถ้อยคำ · รูปแทน · ตำแหน่งคำตอบ+โครงตัวเลือก · surface cues.
<!-- parallel:end -->

**โจทย์:** …

ก. … ข. … ค. … ง. …

**เฉลยที่เสนอ:** …

**เหตุผล / working solution:** แก้ใหม่ตั้งแต่ต้น. (parallel: **ห้าม**นำคำตอบเดิมมาแปลงตามตัวเลข.)

**ตัวลวงที่วัด:** ตัวเลือกผิด**แต่ละตัว**มาจากความเข้าใจผิดอะไร — ตรวจว่า misconception นั้นให้ค่านั้นจริง.

**เหตุผลความยากเทียบ anchor:** (parallel) ทำไมยังเทียบเคียงเมื่อดู 5 มิติ; ถ้าตั้งใจ `step-up`/`step-down` บอกให้ชัด. (original) ทำไมข้อนี้อยู่ระดับความยากนี้.

**config lock:** เฉพาะข้อ Hard / เขียนตอบ / proof — โครงที่ล็อกก่อนเขียน (paper_role, ภาระ, วิธีบังคับ ฯลฯ).

<!-- ทำซ้ำบล็อกด้านบนต่อหนึ่งข้อใน batch -->

---

## Batch review notes

- แต่ละข้อวัด**คนละพฤติกรรม**ไหม (contrast — กันโจทย์ซ้ำหน้ากันภายใน batch).
- ไม่มีข้อไหนคัดตัวเลข/ประโยคตรงจากแหล่งอ้างอิง.
- (parallel) ไม่มี **leakage**: ไม่มีข้อไหนทำให้เดาคำตอบ/โครงของชุดต้นฉบับได้.
- ตำแหน่งเฉลยใน batch นี้ (`ก/ข/ค/ง`) — จะตรวจการกระจายอีกครั้งตอน whole-paper review.

## Decision requested

สิ่งที่ขอให้ครูตัดสินใน batch นี้ (เลือก variant, ปรับตัวลวง, ฯลฯ).

## Approved decision

(หลังครูอนุมัติ) ครูอนุมัติข้อใดบ้าง + variant IDs ที่ล็อกเข้าสถานะหลัก.
