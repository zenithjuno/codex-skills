interface:
  display_name: "Build Control and Changelog"
  short_description: "Bounded build state, current-truth reconciliation, cold logs, and checkpoints"
  default_prompt: "Use $build-changelog to resume this Mode L coding build from its exact BUILD-CONTROL path, load only the Task Contract and current stage, run doctor for drift, reconcile current truth (what became true AND what stopped being true) at each stage close, and continue to the next genuine gate."
